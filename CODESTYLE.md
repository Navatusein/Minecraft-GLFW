# Стиль подключения библиотек
```C++
// Сначала библиотекри
#include <vector>
#include <memory>
#include <thread>
#include <mutex>

#include <SFML/System/Clock.hpp>

// Потом файлы проекта
#include "../Physics/AABB.h"

#include "Chunk/Section.h"
#include "Chunk/Map.h"
#include "WorldSettings.h"

```

# Стиль написания if 
```C++
if (Условие) { // Фигурные скобачки открываются в строке с if

}
else if (Другое условие) { // Фигурные скобачки открываются в строке с else if

}
else { // Фигурные скобачки открываются в строке с else

}


```

## Стиль написания класов
```C++
class FooBar { // Фигурные скобачки открываются в строке с объявление идентификатора класса
    public:
        FooBar();

        void Аoo(); // Название переменных с одного слова
        void Bar();

        void setmNumber // Сеттеры и геттеры називаються первое слово с маленькой

    protected:
        void BarFoo(); // Название переменных с нескольки слов

        int FooNumber // Если переменная называется с 2 слов

    private:
        void FooBar();

        int mNumber; // Если переменна называется с 1 буквы и слова
};


FooBar::FooBar(FooNumber) : FooNumber(FooNumber){}; // Премер установки значений переменным класса в конструкторе 

```

## Стиль написания функций
```C++
void Foo() { // Фигурные скобачки открываются в строке с объявление идентификатора функции

}

```

## Стиль написания комментариев
```C++
// Если комментарий в одну строку

/*
    Если комментарий состоит, 
    из нескольких строк
*/

```

## Стиль проверки ошивок в консоль
```C++
if (SomeError) {
    // [Имя модуля] Текст ошибки
    std::cout << "[SomeModul] error 404" << std::endl;
}

```


